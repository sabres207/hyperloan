{
  "name": "hyperloan",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w server && npm run build -w client",
    "codegen": "npm run codegen -w server && npm run codegen -w client"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "concurrently": "^9.1.2",
    "prettier": "^3.8.3"
  }
}
